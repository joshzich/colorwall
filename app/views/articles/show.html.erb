<div class = "container">
	<div class = "article">
		<h1 class="title">
			<%= @article.title %>
		</h1>

		<p class='body'>
			<%= @article.text %>
		</p>

		<p class='author'>
			<strong>Author:</strong><br>
			<%= @article.user.username %>
		</p>
	</div>
	<h2>Comments</h2>

	<%= render @article.comments %> 

	<h2>Add a comment</h2>
	

		<%= render 'comments/form' %>


	<% if @article.user.id == session[:id] %>
		<%= link_to_if(@article.user.id == session[:id],'Edit', edit_article_path(@article))	%>
	<% end %>
	<%= link_to 'Back', articles_path %>
</div>